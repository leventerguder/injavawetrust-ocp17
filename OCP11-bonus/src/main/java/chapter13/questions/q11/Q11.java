package chapter13.questions.q11;

public class Q11 {

    // What properties of applying @SafeVargs are correct?

    // By applying the annotation, the compiler verifies that all operations on parameters are safe.
    // The annotation can be applied to abstract methods.
    // +++ The annotation can be applied to method an constructor declarations.
    // +++ When the annotation is applied to a method, the method must contain a varargs parameter.
    // The annotation can be applied to method and constructor parameters.
    // +++ The annotation can be applied to static method.
}
