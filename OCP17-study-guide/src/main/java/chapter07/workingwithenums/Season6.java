package chapter07.workingwithenums;

public enum Season6 {

    ;

    private Season6() {

    }
    // WINTER, SPRING, SUMMER, FAIL;

    // You might have noticed that in each of these enum examples,
    // the list of values came first. This was not an accident.
    // Whether the enum is simple or complex, the list of values always comes first.
}
