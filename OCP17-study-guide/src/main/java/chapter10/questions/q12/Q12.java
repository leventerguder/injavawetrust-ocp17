package chapter10.questions.q12;

import java.util.Set;
import java.util.stream.Stream;

public class Q12 {
    public static void main(String[] args) {

//        Set<String> birds = Set.of("oriole", "flamingo");
////        Stream.concat(birds.stream(), birds.stream(), birds.stream()) // DOES NOT COMPILE
////                .sorted() // line X .distinct()
////                .findAny().ifPresent(System.out::println);

        //  Stream.concat() takes two parameters, not the three provided.
    }
}
