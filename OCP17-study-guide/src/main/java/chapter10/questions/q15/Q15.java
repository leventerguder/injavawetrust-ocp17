package chapter10.questions.q15;

public class Q15 {

    /*
    Given the generic type String, the partitioningBy() collector creates a
     Map<Boolean, List<String>> when passed to collect() by default.
     When a downstream collector is passed to partitioningBy(),
     which return types can be created? (Choose all that apply.)
     */

    public static void main(String[] args) {

        // Map<Boolean, List<String>>
        // Map<Boolean, Set<String>>
    }
}
