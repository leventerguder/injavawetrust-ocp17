package chapter04.questions.q06;

public class Q06 {

    public static void main(String[] args) {

        // How many of these lines contain a compiler error?

        double one = Math.pow(1, 2);
        // int two = Math.round(1.0); // DOES NOT COMPILE
        // float three = Math.random(); // DOES NOT COMPILE
        // var doubles = new double[]{one, two, three};
    }
}
